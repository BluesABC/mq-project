# mq-project 工作报告

> 周期：2026-08-19 ~ 2026-08-24 · 阶段：P3 生产化
> 用途：记录本项目工作内容、进度与交付物，供周报/总结引用。

## 1. 本期工作目标

为自研高可用、高并发消息队列服务（C++17）完成 **P0 骨架阶段**：确定目录结构、完成全量文档体系设计，为 P1 MVP 编码提供依据。

## 2. 已完成工作

### 2.1 需求与约束
| 事项 | 说明 |
|------|------|
| 需求评审选型 | 确认技术栈：自研 epoll/io_uring Reactor + 磁盘 WAL 持久化 + 最小依赖（spdlog/gtest） |
| 需求文档 `PRD.md` | 功能需求编号化（F01-F43，含优先级 P0/P1/P2）、非功能需求（性能/可用性/安全）、里程碑、Out of Scope、成功度量 |
| 项目约束 `AGENTS.md` | 27 条硬性约束：语言风格、并发正确性、架构分层、错误码/日志、内存安全、协议版本协商、工程流程与 CI/CD、DoD |

### 2.2 设计文档
| 文档 | 内容 |
|------|------|
| `docs/architecture.md` | 架构总览：多线程 Reactor 模型、分层职责、WAL+Segment 存储、复制设计、里程碑 |
| `docs/design-details.md` | 模块内部实现细节：存储引擎（WAL+mmap 字节布局/单写者/崩溃恢复）、网络层（epoll ET/io_uring/背压）、并发模型（线程池/无锁 MPMC/工作窃取）、内存管理（内存池/对象池/零拷贝） |
| `docs/api-spec.md` | 协议字段布局、命令与状态码枚举（含版本协商 VERSION_MISMATCH） |
| `docs/test-plan.md` | 测试层次、单测/集成测/压测用例规划 |
| `docs/deployment.md` | 部署拓扑、15 项配置、运维操作、容量规划、监控告警、灾难恢复（RPO/RTO） |
| `docs/performance-baseline.md` | 性能基线模板（P1 实测填充） |

### 2.3 交付物
- 仓库目录结构（include/src/test/bench/tools 全部就位，含 `src/core/storage` 等子目录）。
- 构建骨架：`CMakeLists.txt`、`.clang-format`（P0 占位，源码就绪后恢复 `add_subdirectory`）。
- `CHANGELOG.md`、`tools/RELEASE_CHECKLIST.md`（发布检查清单）。

## 3. 工作进度

### 3.1 里程碑进度
| 阶段 | 范围 | 状态 |
|------|------|------|
| P0 骨架 | 目录、构建、文档 | ✅ 完成 |
| P1 MVP | 单机持久化 Broker + SDK | ✅ 完成 |
| P2 高可用 | 复制、ack、故障切换 | ✅ 完成 |
| P3 生产化 | 监控、限流、配额、管理工具与稳定性验证 | 🔄 进行中 |

### 3.2 文档完成度
- 需求/约束/架构/协议/测试/部署/性能基线/变更日志/发布清单：**10 份全部完成**。

## 4. 文档审查与整改

- 对 AGENTS.md 与 PRD.md 进行专项审查，评分 75/100 与 80/100。
- 整改内容：
  - AGENTS.md 补 CI/CD 流水线、代码审查流程、错误码/日志/内存/安全规范、协议版本协商（§3.5-3.7、§5）；
  - PRD.md 补消息 TTL、消费者重试、分区再平衡、心跳、背压策略、Schema 演进、监控指标、幂等细节、压缩、自动位点提交（F25-F35、F16-F17）；
  - 新增部署/性能基线/变更日志/发布清单四份文档。
- 审查结论：缺陷已全部闭环。

## 5. 下一步计划

1. **P1 编码**（按 `docs/design-details.md` §6 依赖拓扑）：
   - ① `core/buffer`（MPMC/SPSC）+ `core/thread_pool`；
   - ② `core/storage`（WAL + mmap 段文件）；
   - ③ `protocol` 编解码；
   - ④ `network`（epoll Reactor）；
   - ⑤ `server`（Broker 装配）；
   - ⑥ `client` SDK；
   - ⑦ `bench`/`tools`。
2. 每模块实现后同步单测，跑通 `ctest`；性能敏感路径进 `bench/`。
3. P1 完成后刷新 `docs/performance-baseline.md` 实测数据并打 v0.2.0 tag。

## 6. 风险与注意事项

- 并发正确性为本项目最大风险：无锁队列内存序、连接固定线程、禁跨线程释放，须按 AGENTS.md §3.2 执行并在 PR 中专人复核。
- 存储格式与协议为契约：任何变更必须先改 `docs/api-spec.md` / `docs/design-details.md`。
- Windows 平台仅保证可编译（select 兜底），性能指标以 Linux 为准。
## 7. 2026-08-19 P1 基础能力进展

### 7.1 已完成

- 新增协议请求/响应帧编解码，完成 magic、版本、长度和 payload 上限校验。
- 新增单分区 WAL 存储引擎，支持追加、按 offset 读取、CRC32 校验、重启恢复和损坏尾部截断。
- 增加 topic、key、value 输入校验，避免非法 topic 进入数据目录路径。
- 恢复 CMake 库和测试目标，新增协议与存储核心测试。
- 已通过 Debug 构建、`/warnaserror` 编译和 `ctest`：1/1 测试通过。

### 7.2 当前里程碑状态

P0 骨架已完成，P1 核心存储和协议基础已开始实现；Broker、epoll Reactor、客户端 SDK、消费者组和主从复制尚未实现，因此当前还不能作为完整消息队列服务部署。

### 7.3 下一步

1. 实现线程池、MPMC/SPSC 队列和内存池。
2. 实现 epoll EventLoop、TCP 连接管理和半包/粘包处理。
3. 组装 Broker 的 topic、produce、fetch 和 offset 提交链路。
4. 增加客户端 SDK、集成测试和性能基准。

## 8. 2026-08-20 P1 并发基础进展

### 8.1 已完成

- 新增固定容量 MPMC 环形队列，槽位状态通过 sequence 序号维护，并使用 acquire/release 内存序发布和回收元素。
- 新增有界线程池，支持队列满时拒绝提交、关闭时排空已接收任务，以及关闭后拒绝新任务。
- 新增队列容量边界、4 生产者/4 消费者一致性、线程池任务排空测试。
- Debug 构建启用 `/warnaserror` 后通过，`ctest` 共 2/2 通过。

### 8.2 后续依赖

线程池和 MPMC 队列可作为 Reactor 到 Worker 的跨线程投递基础。下一步应实现网络读写缓冲与 EventLoop；工作窃取、SPSC 队列和内存池保留为后续性能优化项。

## 9. 2026-08-20 P1 协议流处理进展

- 新增请求流式解码器，支持 TCP 半包和粘包的连续重组。
- 限制累计缓冲和单帧 payload，畸形 header 或长度字段会清空缓存并返回错误。
- 补充分片请求与连续双帧测试，为后续 Reactor 的读缓冲处理提供可复用组件。

## 10. 2026-08-20 P1 Broker 核心进展

- 冻结 `CREATE_TOPIC`、`LIST_TOPIC`、`PRODUCE` 和 `FETCH` 的 P1 命令载荷布局，全部采用大端字节序。
- 新增 `QueueManager`：使用 `shared_mutex` 管理 Topic 元数据，支持分区数校验、稳定 key hash 自动路由与有序 Topic 快照。
- 新增同步 `Broker`：已连通创建 Topic、列出 Topic、key 路由生产、按分区拉取与 WAL 存储。
- Topic 逻辑名支持 UTF-8 校验；WAL 目录使用 Topic 原始字节的十六进制编码，避免路径注入和跨平台文件名冲突。
- 新增进程内端到端测试，已覆盖创建、重复创建、列出、生产、拉取；3 个测试目标重复 10 轮全部通过。

## 11. 2026-08-20 P1 Reactor 调度基础进展

- 新增 `EventLoop`，每个 loop 固定一个 owner 线程，跨线程任务经有界 MPMC 队列投递。
- 已验证回调只在 owner 线程执行，关闭时排空已接收任务且拒绝后续投递。
- 下一步将把 socket 事件注册、连接读写缓冲和 Worker 响应回投接入该 loop。

## 12. 2026-08-20 P1 连接缓冲基础进展

- 新增 owner-thread `MemoryPool`：固定容量，拒绝跨线程分配和超限分配。
- 新增固定容量读写 `Buffer`，满时返回失败供上层触发背压，避免无界增长。
- 新增 `TcpConnection`：读写缓冲仅由 owner loop 修改，Worker 调用 `Send` 时通过 `EventLoop` 回投。
- 新增网络缓冲与连接单测；当前 5 个测试目标连续重复 10 轮均通过。
- 实际 TCP socket 的 accept、非阻塞 read/write 和连接回收尚未接入。

## 13. 2026-08-20 P1 Socket 后端调度准备

- `EventLoop` 新增 idle 轮询回调，回调始终在 owner 线程执行，并以 5ms 等待窗口驱动。
- Windows `select` 兜底和 Linux epoll 后端将复用该入口，避免为 socket I/O 增加会跨线程访问连接状态的工作线程。
- 本轮未接入真实 socket；下一步实现 `TcpServer` 的监听、accept 和非阻塞读写。

## 14. 2026-08-20 P1 日志基础进展

- 新增线程安全 `mq::core::Logger`：支持 trace 到 critical 级别过滤、stderr 输出和单文件滚动。
- 日志单条限制 4KiB，仅在 Broker 初始化与错误路径接入，未记录消息 payload。
- 新增过滤与滚动单测；当前 6 个测试目标全部通过。

## 15. 2026-08-20 P1 TCP 服务进展

- 新增 Windows `select` 兜底 TcpServer：非阻塞 listen、bind、accept、读写与连接回收均在 EventLoop owner 线程。
- 每连接通过 TcpConnection 流式解码协议帧，请求投递 Worker；Worker 编码响应后回投连接写缓冲。
- 新增回环集成测试，单服务器同时维持 100 个连接并完成请求/响应，`ctest -R tcp` 通过。
- Linux epoll 后端和多 Sub Reactor 分发仍为后续工作。

## 16. 2026-08-20 项目进度总览与未完成清单

### 16.1 当前结论

项目已完成 P0，并完成 P1 的协议、WAL 基础、并发原语、Topic 元数据持久化、进程内 Broker 和 Windows TCP 验证链路。当前不是可部署的消息队列服务：核心功能覆盖持久化 Topic 元数据与基础 produce/fetch，但生产 Linux 网络后端、消费者位点恢复、客户端 SDK 和运维能力均未完成。

### 16.2 已完成范围

- 协议：请求/响应编解码、帧长度与版本校验、半包/粘包流式解码；已冻结创建 Topic、列出 Topic、生产和拉取的 P1 载荷布局。
- 存储：单分区 WAL 追加、读取、CRC32 校验、损坏尾部截断恢复，以及 Topic 输入与路径安全编码。
- 并发与内存：有界 MPMC 队列、有界线程池、owner-thread 内存池、固定连接读写缓冲和跨线程响应回投。
- 服务端：Topic 创建、列出、删除、key 路由、生产与拉取的同步 Broker 处理链路；Topic 名和分区数以原子全量快照持久化并可在启动时恢复；Windows `select` 非阻塞 TCP 服务已验证 100 个回环连接。
- 可观测性：线程安全 Logger，支持级别过滤、stderr 输出和单文件滚动。
- 验证：当前 CTest 共 7 个目标通过，包含协议/存储、并发、Broker、EventLoop、网络缓冲、日志和 100 连接 TCP 回环测试。

### 16.3 P1 必须完成的缺口

1. `DELETE_TOPIC`、`COMMIT_OFFSET`、`HEARTBEAT`、消费者组和消费位点持久化。
2. 生产幂等表、批量生产、ack=0/ack=1 的完整语义；ack=all 依赖 P2 复制。
3. WAL 段滚动、稀疏索引、跨段读取、fsync 策略、保留与清理；当前为单日志文件和进程内消息索引。
4. Linux epoll Main/Sub Reactor 和多 Reactor 连接分发；当前仅 Windows `select` 兜底实现。
5. Broker 可执行入口、配置文件、优雅停机和真实 TCP 到 Broker 的完整生产/消费集成测试。
6. 客户端 SDK、重试/连接管理，以及 `bench/` 性能基线；当前没有可发布客户端或吞吐数据。

### 16.4 P2/P3 后续范围

- P2：主从复制、ack=all、选主/故障切换、节点与客户端自动重连。
- P3：监控指标、限流、配额、运维工具、压测门禁和生产发布流程。

### 16.5 当前风险

- 现有 Windows TCP 后端是功能验证实现，不满足 Linux 生产环境的 epoll 吞吐与多 Reactor 目标。
- 消费位点元数据尚未持久化，Broker 重启后无法恢复消费组进度。

## 17. 2026-08-20 Topic 元数据持久化进展

- 新增 `TopicMetadataStore`，使用 `data/metadata/topics.meta` 保存 Topic 全量快照。格式为带 magic 和版本的二进制记录，字段采用大端字节序。
- Topic 创建、删除在 Broker 元数据临界区内完成：内存变更后先落盘临时文件，再原子替换正式文件；写入失败时回滚对应内存变更。
- Broker 初始化时加载快照并整体恢复 `QueueManager`。新增创建 3 个 Topic 后重构 Broker 的恢复测试，完整校验 Topic 名、排序及分区数。
- Debug 配置下 `cmake --build build -j` 通过；`ctest --test-dir build -C Debug --output-on-failure` 共 7/7 通过。
- 存储实现尚未具备文档承诺的 segment/mmap/fsync/清理能力，不能声称达到可靠性和性能目标。
- 本轮大量 P1 改动仍在工作区，尚未形成提交或推送到远端。

## 18. 2026-08-21 客户端 SDK 与性能实测进展

### 18.1 已完成

- 新增 `MqProducer` / `MqConsumer` SDK，支持连接、Topic 创建、生产、批量生产、拉取、提交位点、flush 和关闭。
- Producer 支持递增 sequence 与 `producer_id`，Broker 按 `(producer_id, sequence)` 去重并缓存响应。
- 支持 ack=0、ack=1；ack=all 在当前无复制阶段返回 `NOT_SUPPORTED`。
- 新增 `mq_bench` produce/consume 工具，输出总耗时、TPS、平均延迟、p50、p99、p999。
- 新增 SDK 回环测试、批量生产测试和幂等生产测试；当前 CTest 共 8/8 通过。

### 18.2 本次实测

截图中的 Release 单机回环生产命令为：

```text
mq_bench.exe produce --topic test --messages 1000000 --size 256
```

实测结果：

| 指标 | 数值 |
|------|------:|
| 消息数 | 1,000,000 |
| 消息大小 | 256 B |
| 总耗时 | 655.331 s |
| 生产吞吐 | 1,525.95 TPS |
| 平均延迟 | 654.879 us |
| p50 | 663.047 us |
| p99 | 750.318 us |
| p999 | 837.762 us |

结果没有显示明显异常：p99 和 p999 相对平均延迟仅小幅上升，未见长尾失控。该结果是当前工具默认每批 100 条、单机单进程、单分区、ack=1 条件下的实测值，不能直接与 PRD 中 batch=1000、3 分区、≥10w TPS 的目标比较；后续应按目标参数补测并记录硬件、操作系统和磁盘信息。

### 18.3 当前状态

- P1 已具备存储、Broker、TCP Reactor、配置入口、客户端 SDK、幂等、批量生产和 bench 基础能力。
- 性能基线文档仍需在固定硬件和标准参数下正式刷新；当前截图数据作为单机回环参考值保留。
- 工作区改动尚未形成提交或推送到远端。

## 19. 2026-08-22 Linux 性能测试工具修正与验证

### 19.1 已完成

- 修复 `mq_bench` 参数问题：此前未知参数会被静默忽略，`--connections 10` 实际仍是单连接测试。
- 新增并实际接入参数：`--connections`、`--batch`、`--partitions`；输出会回显实际生效配置。
- Producer 基准改为多线程、多 TCP 连接，并为每个连接分配独立 Producer ID；消息 key 携带连接和序号，支持多分区路由。
- Consumer 支持按指定分区订阅，bench 可按连接数分配分区消费任务。
- 增加 `MQ_ENABLE_SANITIZERS` CMake 选项。GCC/Clang 下启用 ASan/UBSan 和 frame pointer，MSVC 下保持关闭。
- 补充 Linux 标准基准矩阵和 sanitizer 执行命令到 `docs/performance-baseline.md`、`docs/test-plan.md`。

### 19.2 验证结果

- Windows Debug 构建通过，CTest 8/8 全部通过。
- 多连接回环验证：

```text
mq_bench produce --topic bench_multi --messages 1000 --size 256 --connections 10 --batch 20 --partitions 3
mode=produce messages=1000 size=256 connections=10 batch=20 partitions=3 total_seconds=0.103615 TPS=9651.11 avg_latency_us=712.143 p50_us=594.915 p99_us=1553.18 p999_us=1553.18
```

- 当前 Windows 环境无可用 Linux/WSL 发行版，因此未在本机执行 Linux CTest、Linux ASan/UBSan；不能将本次 Windows sanitizer 选项配置构建视为 sanitizer 运行验证。

### 19.3 Linux 执行清单

```bash
cmake -S . -B build-linux -DMQ_BUILD_TESTS=ON -DMQ_BUILD_SERVER=ON -DMQ_BUILD_BENCH=ON
cmake --build build-linux -j
ctest --test-dir build-linux --output-on-failure
cmake -S . -B build-linux-sanitize -DMQ_BUILD_TESTS=ON -DMQ_ENABLE_SANITIZERS=ON
cmake --build build-linux-sanitize -j
ctest --test-dir build-linux-sanitize --output-on-failure
```

标准性能测试使用 `docs/performance-baseline.md` §4.1 的矩阵命令；正式基线仍需补录 Linux 硬件、内核、磁盘和每组 3 次中位数结果。

新增 `tools/run_bench_matrix.sh`，可在 Linux Broker 运行后自动执行 5 组生产基准、重复指定次数并输出 CSV 原始结果；正式报告应从该 CSV 取各场景中位数后回填性能基线表。

## 20. 2026-08-22 基准完成数边界修复

- 定位到 Linux 端 `completed=1000001` 的根因是旧版 `mq_bench` 二进制仍在执行旧的并发计数逻辑；新版本增加全局批次分配器，最后一批按剩余消息数截断，生产完成数不会超过目标值。
- 消费模式也改为有上限的原子 CAS 计数，多个连接同时收尾时不会把统计值递增到目标值以上。
- 基准输出增加 `bench_version=2`，用于确认 Linux 端确实运行了重新构建的可执行文件。
- Windows Debug 构建、8/8 CTest 及 Broker 回环冒烟验证通过；冒烟结果为 1000 条消息严格完成，TPS 约 10425。
- Linux 端需从更新后的源码重新构建 `build-linux/mq_bench` 后再运行矩阵，不能直接复用旧可执行文件。

## 21. 2026-08-23 Linux 基准启动异常处理

- 针对 Linux 基准运行时 `std::system_error: Invalid argument`，修正自动 Reactor/Worker 线程数：默认仍依据 CPU 核心数，但限制为最多 32 个，避免虚拟机暴露大量逻辑核而超过线程资源配额。
- ThreadPool 和 EventLoop 增加线程创建失败时的已创建线程回收及资源清理，启动异常不再遗留线程或直接无诊断终止。

- 进一步定位到 epoll fd 回调原先未捕获异常；异常从 Reactor 线程逃逸时会触发 `std::terminate`。现已在 EventLoop 分发 fd 回调处统一捕获，并移除异常连接的 fd 注册，避免 Broker 进程被单个请求线程终止。

- 定位并修复性能矩阵最后一个场景的失败：`4096 B` 消息使用 `batch=1000` 时单请求约 4 MiB，超过协议 `1 MiB` payload 上限，导致所有 Worker 第一批请求失败并报告 `completed=0`。bench 现按消息大小自动将批量限制到协议允许值；该场景实际批量为 251。

- 根据 Linux `strace` 的 `clone3` 成功后 `SIGSEGV` 结果，定位到 `mq_bench` Producer 的 `next_message` 原子变量生命周期错误：Worker 在外层 `join()` 前已离开变量所在代码块。现将该原子变量提升到与 Worker 同生命周期，消除多连接压测下的 use-after-scope。

## 22. 2026-08-23 Linux 性能矩阵完成

- `tools/run_bench_matrix.sh build-linux 1000000 3` 共完成 5 个场景、每场景 3 轮，全部严格完成 1,000,000 条消息，无崩溃、无计数超出。
- `bench_version=3` 已确认运行的是修复后的基准程序；4096 B 场景自动将单请求批量从 1000 限制为 251，以符合 1 MiB 协议 payload 上限。

| 场景 | TPS 中位数 | p99 延迟中位数 |
|------|-----------:|---------------:|
| 256 B / batch 1 / conn 1 | 6,266.80 | 441.627 us |
| 256 B / batch 100 / conn 1 | 227,089.00 | 7.552 us |
| 256 B / batch 1000 / conn 1 | 257,637.00 | 4.444 us |
| 256 B / batch 1000 / conn 10 | 380,918.00 | 213.134 us |
| 4096 B / effective batch 251 / conn 10 | 147,034.00 | 167.802 us |

- 多连接 256 B 场景首轮约 29,398 TPS，后两轮约 380,918/457,393 TPS，存在明显预热或磁盘状态影响；正式性能对比应记录硬件、磁盘、Broker 启动状态并优先使用三轮中位数，不能使用单轮峰值作为基线。

## 23. 2026-08-23 进入 P2 高可用阶段

- P1 收尾矩阵完成：5 个场景各 3 轮全部成功，结果已记录性能基线；Linux CTest 和 sanitizer 仍由 Linux 环境执行。
- 新增 `ReplicationCoordinator`，维护 Leader/Follower/Candidate 角色、Follower 心跳健康状态、复制 offset、quorum 判断和确定性选主。
- 新增 `mq_replication_tests`，覆盖副本确认、心跳超时、角色切换和副本快照排序；Windows Debug CTest 已由 8/8 增至 9/9 并全部通过。
- 当前 P2 仍处于复制基础层：跨节点 TCP 日志传输、Follower 增量拉取、选举协议和 `ack=all` 实际确认尚未完成；在传输层接入前 `ack=all` 继续返回 `NOT_SUPPORTED`。

## 24. 2026-08-23 P2 复制数据平面基础

- 新增内部 `REPLICA_FETCH(0x40)` 和 `REPLICA_APPEND(0x41)` 命令，必须携带 `REPLICATION` flag，普通客户端请求会被拒绝。
- Leader 可按 topic/partition/offset 导出增量消息；Follower 按连续 offset 追加 WAL，拒绝重复 offset、跳 offset 和非法消息。
- 新增 Broker 双实例复制单测，验证 Leader 写入、Follower 拉取、Follower WAL 恢复读取及重复追加拒绝；Windows Debug CTest 仍为 9/9 全部通过。
- 当前尚未接入节点间 TCP ReplicationClient、周期拉取任务、选举消息和自动故障切换；`ack=all` 仍保持 `NOT_SUPPORTED`，避免宣称尚未具备的零丢失保证。

## 25. 2026-08-23 P2 节点复制与 quorum ack

- 新增 TCP `ReplicationClient`，支持复制 Fetch、Append 和 Heartbeat；新增真实 TCP 集成测试。
- Broker 接入周期复制线程：Follower 每 250ms 拉取各 Topic/Partition 增量并按连续 offset 写 WAL，Leader 周期发送节点心跳并触发健康节点确定性选主。
- `ack=all` 已接入实际 quorum 路径：本地写入后同步向 peer 发送 `REPLICA_APPEND`，达到 quorum 返回成功，peer 不可达返回 `STORAGE_ERROR`。
- Windows Debug 构建和 CTest 10/10 全部通过，包含双 Broker TCP 复制、心跳、peer 下线和 ack=all 验收。
- 当前自动选主仍是确定性健康节点选择，不提供 Raft/ZAB 的任期和网络分区安全保证；下一阶段应补多数派选举、日志提交索引和真正的自动对外切换。

## 26. 2026-08-23 P2 任期、提交与分区安全

- 复制协调器增加单调递增任期、投票记录、Candidate 多数票晋升、Leader 身份和 commit index/last applied 状态。
- 新增 `REPLICA_VOTE(0x42)`；复制 Append/Heartbeat 可携带任期、Leader 标识和提交索引，低任期请求被拒绝。
- Broker 写入增加 Leader 和多数派可达门禁；`ack=all` 在同一任期复制成功后推进提交索引，无法形成多数派时返回错误。
- Follower 失去 Leader 后周期发起选举；SDK 支持多个 Broker endpoint，连接失败时轮换 endpoint 并继续指数退避重连。
- 已用 MSVC 直接完成复制协调器、Broker、ReplicationClient、SDK 和复制单测的编译验证。完整 CTest 受当前桌面环境 `PATH/Path` 重复键影响，需在清洁 VS 开发者环境重跑。

## 27. 2026-08-24 P2 对外故障切换补强

- SDK 收到 `NOT_LEADER` 响应后会关闭当前连接、轮换到下一个 Broker endpoint，并在请求超时窗口内重试；不再只处理 TCP 连接失败切换。
- Follower 复制周期增加独立 Leader 心跳探测，即使 Topic 为空也不会因“没有拉到消息”误判 Leader 失联并发起选举。
- 选票响应仅接受已注册副本节点，避免无效节点伪造多数票。
- 新增客户端多 endpoint 故障切换测试场景：首节点返回 `NOT_LEADER`，Producer 自动切换到第二节点并成功生产。

## 28. 2026-08-24 WSL2 Linux 验证

- 在 WSL2 Ubuntu（Linux 5.10.16.3-microsoft-standard-WSL2，GCC 13.3.0）完成 `build-linux` 构建，编译通过。
- Linux 普通 CTest 全部通过：10/10。
- Linux ASan/UBSan 构建和全量 CTest 全部通过：10/10，未发现 sanitizer 报告。
- 启动 `mq_broker` 监听 `127.0.0.1:9092`，使用 `mq_bench` 通过真实 TCP 生产 1000 条消息，2 连接、batch=100、3 分区，TPS 2824.74。
- 发送 SIGINT 后 Broker 正常退出并释放监听端口，完成 Linux 进程级冒烟验证。
- 该小规模 TPS 仅作为功能冒烟数据，不替代标准性能矩阵；正式基线仍需执行 `tools/run_bench_matrix.sh build-linux 1000000 3`。

## 29. 2026-08-24 P3 可观测性基础

- 新增 `METRICS(0x04)` 只读管理查询，返回 Prometheus 文本格式，不改变现有业务载荷。
- Broker 新增请求、生产、拉取、错误计数，以及复制任期、commit index 和角色指标。
- 指标通过现有 TCP 协议获取；部署文档明确了由外部 exporter 提供 HTTP `/metrics` 的方式。
- 新增 Broker 指标单测，下一步继续补充限流、配额和管理工具。

## 30. 2026-08-24 P3 生产请求限流

- 新增 `produce_rate_limit` 配置，按秒限制普通 PRODUCE/PRODUCE_BATCH 请求，默认 `0` 表示关闭。
- 超过窗口返回 `RATE_LIMITED`；复制内部请求绕过生产限流，避免副本同步被业务流量门禁阻断。
- 批量请求按请求计数，不按批内消息数重复扣减限额。
- 新增 Broker 限流单测；下一步继续补充配额和管理工具。

## 31. 2026-08-24 P3 Topic 生产字节配额

- 新增 `topic_produce_quota_bytes` 配置，按 Topic 维护 1 秒生产字节窗口，默认 `0` 表示不限额。
- 单条 PRODUCE 统计 key/value 实际字节；PRODUCE_BATCH 在完整解析后统计未命中幂等缓存的整批字节，并在 WAL 写入前一次性预留。
- 超限返回新增 `QUOTA_EXCEEDED(0x19)`；复制内部请求绕过配额，重复幂等请求不重复计费。
- `METRICS` 新增 `mq_topic_produce_quota_bytes_per_second` 和 `mq_topic_produce_bytes_used`。
- 新增单测覆盖批量超额不部分写入、单条配额和指标；Windows Debug、WSL2 Linux 普通构建/CTest、WSL2 ASan/UBSan 构建/CTest 均已通过（10/10）。

## 32. 2026-08-24 P3 管理查询工具

- 新增 `mq_admin` 可执行工具，支持 `topics` 查询 Topic 元数据、`metrics` 查询 Prometheus 文本指标。
- SDK 新增只读 `listTopics`/`metrics` 调用，复用现有 TCP 超时、自动重连和协议编解码。
- CMake 新增 `MQ_BUILD_TOOLS` 选项，默认构建 `mq_admin`；新增 `mq_admin_help` CTest。
- Windows 真实 Broker TCP 查询、Windows Debug CTest 11/11、WSL2 Linux CTest 11/11、WSL2 ASan/UBSan CTest 11/11 均通过。

## 33. 2026-08-24 P3 稳定性验证与生命周期修复

- 新增 CRC 损坏尾部、半条 WAL 记录恢复、消费者位点快照独立性测试。
- 新增客户端顺序消费以及 `ack=0`、`ack=1`、`ack=all` 语义测试；修正测试对 ack=0 不等待服务端处理的错误假设。
- ASan/UBSan 发现并定位连接关闭后异步写回访问已释放 MemoryPool 的 use-after-free；服务端连接现持有 MemoryPool 所有权，确保异步回调结束后再释放缓冲池。
- Windows Debug、WSL2 Linux、WSL2 ASan/UBSan CTest 均通过（11/11）。
- P3 尚余固定硬件上的标准性能矩阵和性能基线正式刷新，不能用功能测试结果替代。

## 34. 2026-08-24 P3 性能回归门禁

- 新增 `tools/bench-baseline.csv`，记录基于既有 WSL2 矩阵结果的保守 TPS/p99 门槛。
- 新增 `tools/check_bench_matrix.sh`，校验 5 个生产场景、每场 3 轮、TPS/p99 字段和回归阈值；缺场景、缺轮次或指标越界均返回失败码。
- 新增 Linux CTest fixture，验证性能结果校验脚本；WSL2 Linux 普通和 ASan/UBSan CTest 均通过（12/12），Windows Debug CTest 通过（11/11）。
- 正式发布前仍需在固定 CPU、内存、磁盘和内核环境下重新执行矩阵，并据此替换样本基线。

## 35. 2026-08-24 VM16 Release 测试修复

- 定位 VM16 `mq_core_tests` 的 `std::out_of_range`：Release 默认定义 `NDEBUG`，测试断言未执行，协议编码失败后继续对空帧执行 `substr(9)`。
- CMake 对所有 CTest 目标加入 `-UNDEBUG`/`/UNDEBUG`，确保 Release 测试保留断言验收条件。
- WSL2 Linux Release 配置 `build-vm16` 构建并运行 CTest 12/12 通过，包含性能矩阵校验测试。

## 36. 2026-08-24 消费基准失败定位与网络大响应修复

### 36.1 根因

- `mq_bench` 的 Producer ID 固定为 `1000 + connection`，连续场景共用一个 Broker 时，前一场景的幂等缓存会让后续 Topic 的请求被错误地当作重复请求，返回缓存成功结果但不追加 WAL。
- Fetch 按 key/value 字节数限制消息数量，但响应还包含 offset、时间戳和长度字段；响应帧超过协议 1MiB 上限时编码失败。
- 即使 Broker 生成了合法的大 Fetch 响应，TcpConnection 原 64KiB 固定写缓冲也会拒绝排队，客户端最终表现为请求超时。
- 消费 bench 每条消息提交一次位点，1,000,000 条消息会产生大量 metadata 原子替换，掩盖实际消费吞吐。

### 36.2 修复

- 每次 bench 进程使用基于启动时间的唯一 Producer ID；保留 Broker `(producer_id, sequence)` 幂等语义，避免修改协议契约。
- Broker Fetch 构造响应时再次按协议 payload 上限裁剪，保证响应可编码。
- TcpConnection 改为受容量限制的完整响应帧写队列，容量覆盖协议最大响应帧；读缓冲和连接 MemoryPool 仍保持有界，连接关闭后的异步回投仍由 `shared_ptr` 保活。
- Consumer bench 启动前读取 Topic 实际分区数，连接数不超过分区数；按 batch 周期提交位点，退出前补交最后未提交位点；连接、订阅、Fetch 超时和提交失败均输出具体诊断。
- SDK Consumer 缓存单次 Fetch 返回的整批消息，后续 `poll()` 直接顺序消费缓存，避免百万消息场景反复请求同一批剩余数据。
- 同步更新 `docs/design-details.md`、`docs/performance-baseline.md` 和 `CHANGELOG.md`。

### 36.3 验证结果

- Windows Debug：`cmake --build build --config Debug` 通过，CTest 11/11 全部通过。
- WSL2 Linux：普通 CTest 12/12 全部通过；ASan/UBSan CTest 12/12 全部通过。
- Windows 真实 TCP：1,000 条、256B、3 连接、batch=100、3 分区，生产和消费均通过；消费 TPS 1,101.15。
- WSL2 真实 TCP：1,000 条、256B、3 连接、batch=100、3 分区，生产和消费均通过；消费 TPS 4,803.93。
- WSL2 真实 TCP：10,000 条、256B、3 连接、batch=1000、3 分区，生产和消费均通过；消费 TPS 12,232.1，验证批量 Fetch 缓存生效。
- VM16 生产矩阵原始结果 `bench-results-20260824-203936.csv` 的 5 个场景均完成 1,000,000 条并通过门禁；VM16 1,000,000 条消费基线需用修复后构建重新执行并回填。

### 36.4 当前进度

- P1、P2 已完成；P3 监控、限流、Topic 配额、管理工具、稳定性测试和性能回归门禁已完成。
- P3 剩余验收项为 VM16 本地磁盘上的 1,000,000 条消费基线，以及基于该结果刷新消费吞吐/p99 正式门槛。
