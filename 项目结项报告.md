# mq-project 项目结项报告

## 1. 基本信息

| 项目 | 内容 |
|---|---|
| 项目名称 | mq-project 自研高可用消息队列 |
| 技术栈 | C++17、CMake、TCP、Linux epoll、Windows select、WAL、GCC/MSVC |
| 结项阶段 | P1 MVP、P2 高可用、P3 生产化 |
| 结项日期 | 2026-08-24 |
| 代码仓库 | https://github.com/BluesABC/mq-project |
| 主分支 | `main` |
| 最新提交 | `5536a86 docs(test): 记录VM16完整验收流程` |
| 结项状态 | 当前验收范围通过；生产化专项指标中保留部分 TBD |

## 2. 项目目标

项目目标是交付一个基于 C++17 的高可用消息队列系统，包含 Broker、客户端 SDK、持久化 WAL、Linux epoll Reactor、Windows select 后端、节点复制、故障切换、性能基线和运维工具。

本报告用于确认已实现功能、测试证据、性能结果、未量化项目和后续工作边界，作为项目验收材料。

## 3. 交付范围

### 3.1 P1 单机消息队列

- Broker 可执行入口、INI 配置解析、日志初始化和优雅停机。
- Topic 创建、删除、列表查询和元数据原子持久化。
- PRODUCE、PRODUCE_BATCH、FETCH、COMMIT_OFFSET、HEARTBEAT、DELETE_TOPIC、METRICS 请求处理。
- WAL 顺序追加、CRC 校验、崩溃恢复、半条记录截断和跨段读取。
- Segment 按配置大小滚动，默认 64MiB；文件名为 20 位十进制补零 `.log` 文件。
- 每 1000 条消息写入一条稀疏索引，索引文件使用 `.index` 后缀。
- fsync 支持 per-message、per-batch、interval 策略，默认 per-batch。
- Segment 按时间、大小保留策略后台清理。
- 消费者组和消费位点原子持久化到 `metadata/consumer_offsets.meta`。

### 3.2 网络与并发

- EventLoop 基于 Linux `epoll_create1`/`epoll_wait`，支持 `EPOLLIN`、`EPOLLOUT`、`EPOLLET`。
- Main Reactor 负责 accept，并以 Round-Robin 分发连接到 Sub Reactor。
- Sub Reactor 独立 epoll 实例，连接固定归属同一 Reactor 线程。
- Windows 使用非阻塞 Winsock select 后端。
- 请求半包/粘包解码、Worker 业务处理和 Reactor 线程回写。
- 连接关闭后的异步回投由 `shared_ptr` 保活，避免 MemoryPool 生命周期竞态。
- 写队列支持协议最大响应帧并保留有界背压。

### 3.3 客户端 SDK 与性能工具

- `MqProducer`：连接、Topic 创建、单条生产、批量生产、flush、关闭。
- `MqConsumer`：连接、订阅、poll、commit、关闭。
- 自动重连、指数退避和默认 5 秒请求超时。
- Producer sequence number 和 Broker 幂等缓存。
- ack=0、ack=1、ack=all 语义；ack=all 经过 quorum 确认。
- Consumer 缓存批量 Fetch 响应，避免重复网络请求。
- `mq_bench` 输出 TPS、平均延迟、p50、p99、p999。
- `mq_admin topics` 和 `mq_admin metrics` 运维查询工具。

### 3.4 P2 高可用

- 节点间 TCP ReplicationClient。
- Replica Fetch、Append、Heartbeat、Vote 内部协议。
- 周期性增量拉取和心跳探测。
- 任期、Candidate 选举、投票多数派和 commit index。
- 低任期请求拒绝、网络分区下多数派写入保护。
- Leader/Follower 状态切换和客户端多 endpoint 故障切换。
- `ack=all` 实际 quorum 确认，副本不可达时返回错误。

### 3.5 P3 生产化

- Prometheus 文本指标：请求、生产、Fetch、错误、任期、commit index、角色。
- 全局生产请求限流。
- Topic 生产字节配额和配额指标。
- 性能矩阵脚本、结果校验脚本和 CTest 性能门禁。
- Windows、WSL2、VM16 Linux 构建/测试验证。
- 工作报告、性能基线、测试方案、设计文档和变更日志同步更新。

## 4. 架构交付物

| 模块 | 主要实现 | 验证状态 |
|---|---|---|
| `src/core` | WAL、Segment、稀疏索引、Topic、线程池、内存池、元数据 | 已验证 |
| `src/protocol` | 请求/响应编解码、状态码、复制命令 | 已验证 |
| `src/network` | EventLoop、TcpConnection、TcpServer、epoll/select | 已验证 |
| `src/server` | Broker、复制协调器、ReplicationClient、main | 已验证 |
| `src/client` | Producer、Consumer、重连、幂等、批量 Fetch 缓存 | 已验证 |
| `bench` | 生产/消费基准和延迟统计 | 已验证 |
| `tools` | `mq_admin`、矩阵脚本、回归门禁 | 已验证 |

主要文档：

- `PRD.md`
- `AGENTS.md`
- `docs/architecture.md`
- `docs/design-details.md`
- `docs/api-spec.md`
- `docs/test-plan.md`
- `docs/deployment.md`
- `docs/performance-baseline.md`
- `CHANGELOG.md`
- `工作报告.md`

## 5. 测试与验收数据

### 5.1 自动化测试矩阵

| 环境 | 构建类型 | 测试结果 | 结论 |
|---|---|---:|---|
| Windows | Debug | CTest 11/11 | 通过 |
| WSL2 Ubuntu | Debug | CTest 12/12 | 通过 |
| WSL2 Ubuntu | ASan/UBSan | CTest 12/12 | 通过，无 sanitizer 报告 |
| VM16 Linux | Release | CTest 12/12 | 通过 |

测试覆盖包括：

- WAL 追加、恢复、CRC 损坏尾部、半条记录、段滚动、索引、清理和跨段读取。
- Topic 元数据恢复、消费者位点持久化和原子快照。
- MPMC 队列、线程池、MemoryPool 和连接生命周期。
- EventLoop、TCP 连接、Linux epoll、Windows select 和 1000 并发连接回环。
- Producer 幂等、批量生产、Consumer 顺序消费、位点提交和 ack 语义。
- ReplicationClient、心跳、投票、任期、commit index、quorum 和故障切换。
- 限流、Topic 配额、Metrics、管理工具和性能矩阵校验。

### 5.2 VM16 验收流程

VM16 实际验收顺序如下：

```bash
cd ~/mq-project
git pull --ff-only origin main

cmake -S . -B build-vm16 \
  -DMQ_BUILD_TESTS=ON \
  -DMQ_BUILD_SERVER=ON \
  -DMQ_BUILD_BENCH=ON \
  -DMQ_BUILD_TOOLS=ON \
  -DCMAKE_BUILD_TYPE=Release
cmake --build build-vm16 -j"$(nproc)"
ctest --test-dir build-vm16 --output-on-failure
```

启动 Broker：

```bash
./build-vm16/mq_broker --config conf/broker.conf
```

另一个终端执行生产、消费和管理查询：

```bash
cd ~/mq-project
topic="vm16_consume_final_$(date +%Y%m%d%H%M%S)"
group="vm16_consume_final_group_$(date +%Y%m%d%H%M%S)"

./build-vm16/mq_bench produce \
  --topic "$topic" \
  --messages 1000000 \
  --size 256 \
  --connections 3 \
  --batch 1000 \
  --partitions 3

./build-vm16/mq_bench consume \
  --topic "$topic" \
  --group "$group" \
  --messages 1000000 \
  --connections 3 \
  --batch 1000 \
  --partitions 3

./build-vm16/mq_admin topics
./build-vm16/mq_admin metrics
```

Broker 优雅停机：

```bash
kill -TERM "$(pgrep -n mq_broker)"
ss -ltnp | grep 9092 || true
```

### 5.3 VM16 实测结果

| 验收项 | 实测结果 |
|---|---|
| Git 拉取 | `origin/main` 成功同步 |
| Release 构建 | 通过 |
| CTest | 12/12 通过 |
| 生产矩阵 | 5 个场景，每场 3 轮，每轮 1,000,000 条，全部通过 |
| 生产原始结果 | `bench-results-20260824-203936.csv` |
| 消费消息数 | 1,000,000 |
| 消费吞吐 | 723,902 TPS |
| 消费平均延迟 | 3.79205 us |
| 消费 p50 | 0.07 us |
| 消费 p99 | 0.401 us |
| 消费 p999 | 998.266 us |
| 进程级恢复 | Topic 和数据恢复成功 |
| 优雅停机 | Broker 停止监听并退出 |

### 5.4 VM16 生产矩阵中位数

| 场景 | TPS | p50 | p99 | p999 |
|---|---:|---:|---:|---:|
| 256B / batch=1 / 1 连接 / 3 分区 | 5,079 | 177 us | 709 us | 1,380 us |
| 256B / batch=100 / 1 连接 / 3 分区 | 374,668 | 2.28 us | 8.81 us | 17.64 us |
| 256B / batch=1000 / 1 连接 / 3 分区 | 605,479 | 1.46 us | 2.66 us | 4.49 us |
| 256B / batch=1000 / 10 连接 / 3 分区 | 830,343 | 10.77 us | 32.17 us | 46.71 us |
| 4096B / effective batch=251 / 10 连接 / 3 分区 | 137,965 | 66.56 us | 211.49 us | 443.78 us |

## 6. 验收结论

### 6.1 已达成

- 核心功能、持久化、跨段读取、消费者位点、网络 Reactor、SDK、复制和故障切换均已实现并通过自动化测试。
- Windows、WSL2 和 VM16 Linux 均完成构建及测试验证。
- VM16 生产矩阵全部完成；1,000,000 条消费基线达到 `723,902 TPS`。
- 消费吞吐达到 PRD 的 `100,000 TPS` 目标，消费 p99 达到 `10ms` 目标。
- 代码和文档已同步推送至 GitHub `main`。

### 6.2 结项判定

当前用户要求范围内的功能开发、测试验证和交付文档已经完成，可以提交项目验收。以下 TBD 项属于生产环境长期容量和专项性能评估，不构成本次核心功能验收失败。

## 7. TBD 项与风险

| TBD 项 | 当前状态 | 风险 | 建议验证方式 |
|---|---|---|---|
| 5 万连接空闲内存/连接 | 未测 | 资源规划缺少实测依据 | 分阶段建立 1w/3w/5w 连接并记录 RSS、fd、堆内存 |
| 写入放大 | 未测 | 磁盘容量和 SSD 寿命评估不完整 | 对比消息有效载荷与 WAL、索引、元数据实际写入量 |
| 随机 offset 冷段读取 | 未测 | 冷数据读取延迟未建立基线 | 生成多段 WAL，随机 offset 采样 p50/p99/p999 |
| 长轮询消费 | 未测 | 空队列场景 CPU 和延迟未知 | 增加长轮询协议/实现后执行空队列与消息到达测试 |
| 5 万连接长期稳定性 | 未测 | 长时间资源泄漏和连接抖动未覆盖 | 24 小时 soak test，采集连接数、RSS、fd、错误率 |
| 1GiB 段滚动压力 | 功能已测，长期压力未测 | 段切换期间吞吐波动未知 | 小段配置模拟滚动并运行多轮生产矩阵 |
| 清理线程积压 | 功能已测，长期压力未测 | 保留策略下磁盘回收速度未知 | 注入时间/大小过期段，观测扫描周期和删除积压 |
| 内存峰值与写缓冲水位 | 未测 | 高峰期背压阈值需要校准 | 生产/消费不平衡压测并采集 metrics/RSS |
| 关闭抖动与恢复耗时 | 基本验证通过，未形成基线 | 重启 RTO 缺少量化数据 | 记录 SIGTERM、Flush、重启、Topic 可用的时间戳 |

## 8. 后续计划

### P3.1 生产容量专项

1. 完成 5 万连接分阶段压测和 24 小时稳定性测试。
2. 增加内存、fd、连接数、写队列水位和清理积压的长期监控。
3. 建立随机 offset、冷段读取、长轮询和段滚动专项基线。

### P3.2 质量与发布

1. 将 VM16 Release CTest、sanitizer 测试和性能门禁纳入 CI。
2. 为正式版本补充版本号、发布说明和 `tools/RELEASE_CHECKLIST.md` 执行记录。
3. 进行并发、存储格式和复制代码的专项代码审查。
4. 基于 TBD 项结果更新 `docs/deployment.md` 的容量规划和告警阈值。

## 9. 交付物清单

- 源码：`include/`、`src/`、`bench/`、`tools/`。
- 测试：`test/unit/`、`test/integration/`。
- 配置：`conf/broker.conf`。
- 构建：CMake Debug/Release/sanitizer 配置。
- 文档：需求、架构、设计、协议、测试、部署、性能基线、工作报告、变更日志。
- 性能原始结果：`bench-results-20260824-203936.csv`。
- GitHub：`https://github.com/BluesABC/mq-project`，分支 `main`。

## 10. 验收签字

| 角色 | 姓名 | 结论 | 日期 |
|---|---|---|---|
| 开发负责人 |  |  | 2026-08-24 |
| 测试负责人 |  |  | 2026-08-24 |
| 项目验收人 |  |  | 2026-08-24 |
