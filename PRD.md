# mq-project 需求文档（PRD）

> 版本: v0.2 · 更新: 2026-08-19
> 关联文档：`docs/architecture.md`（架构）、`docs/design-details.md`（实现细节）、`docs/api-spec.md`（协议）、`docs/test-plan.md`（测试）。

## 1. 背景与目标

自研一套高可用、高并发的消息队列服务，以 C++17 为主实现。目标是在**不依赖重型开源框架**的前提下，交付一个支撑海量连接、低延迟、可靠持久化的消息中间件，并提供高性能客户端 SDK 与运维工具。

- 核心目标：高性能（10w+ TPS）、可靠持久化（WAL）、高可用（复制与故障切换）。
- 约束目标：最小第三方依赖、可维护的分层架构、协议冻结。

## 2. 目标用户与典型场景

| 场景 | 说明 |
|------|------|
| 微服务解耦 | 服务间异步削峰，消息吞吐优先 |
| 数据管道 | 日志/事件采集 → 分析存储，追求高写入吞吐 |
| 任务队列 | 延迟不敏感、可靠性要求高的异步任务 |
| 金融/订单类 | 要求 ack 强一致（ack=all）与零丢失 |

## 3. 名词定义

| 名词 | 含义 |
|------|------|
| Broker | 消息服务节点（本项目的服务端进程） |
| Topic | 消息的逻辑分类 |
| Partition | Topic 下的分区，存储与并发的基本单位 |
| Offset | 分区内消息的逻辑递增偏移（从 0 起） |
| Consumer Group | 消费组，组内消费者分摊分区消息 |
| Commit | 消费位点提交，推进组内已消费进度 |
| TTL | 消息生存时间，过期消息不可再消费并被清理 |
| Rebalance | 消费组成员变化时分区归属重新分配 |
| WAL | Write-Ahead Log，预写日志（段文件） |
| Segment | 分区下固定大小的日志段文件 |
| fsync 策略 | 落盘时机策略（每条/定时/定量） |
| ack | 生产者收到响应所需的最小副本确认数 |

## 4. 功能需求

### 4.1 Topic 管理
| ID | 需求 | 优先级 |
|----|------|--------|
| F01 | 创建 Topic：名称（UTF-8 ≤ 64B）+ 分区数（1~1024），支持分区数配置 | P0 |
| F02 | 删除 Topic：级联删除所有分区数据与元数据 | P1 |
| F03 | 列出 Topic 及分区状态 | P1 |
| F04 | Topic 元数据持久化，重启不丢失 | P0 |

### 4.2 生产消息
| ID | 需求 | 优先级 |
|----|------|--------|
| F10 | 按 Topic+分区（可指定或 hash key 路由）追加消息 | P0 |
| F11 | 支持 `ack=0 / ack=1 / ack=all` 三档确认语义 | P0 |
| F12 | 批量生产（一次请求多条）以提升吞吐 | P0 |
| F13 | 单条消息大小 1B ~ 1MB | P0 |
| F14 | 消息可带 key（用于分区路由）与 value | P0 |
| F15 | 幂等生产：重复 `request_id` 返回缓存结果 | P1 |
| F16 | 消息压缩（可选）：Topic 级 `compression` 配置（无/快速 zstd 风格），压缩在客户端 SDK 完成，Broker 透明存储 | P2 |
| F17 | Schema 演进：消息帧预留 `schema_version` 字段，Broker 不解析 body 语义，客户端负责兼容；字段变更走协议版本协商 | P2 |

### 4.3 消费消息
| ID | 需求 | 优先级 |
|----|------|--------|
| F20 | 按分区 + 起始 offset 拉取消息（max_bytes 限长） | P0 |
| F21 | 消费组位点独立提交、可查询、可重置 | P0 |
| F22 | 自动跨段回卷读取，保证 offset 连续性 | P0 |
| F23 | 长轮询拉取（无数据时挂起，默认 5s） | P1 |
| F24 | 单分区有序性保证（FIFO），多分区并行消费 | P0 |
| F25 | 位点提交支持手动/自动两模式：`auto_commit`（默认每 5s 提交已消费位点）与手动 `commit_offset` | P1 |
| F26 | 分区再平衡（Rebalance）：组成员加入/退出/订阅变化时，按分区分摊规则重新分配分区，位点按分区保留 | P1 |
| F27 | 消费者重试：消费失败可返回重试标记（预留协议位），Broker 支持可配置的延迟重投（不落地死信队列） | P1 |
| F28 | 心跳：消费者定期发送心跳保持组成员身份，超时（默认 30s）视为离线并触发 Rebalance | P0 |

### 4.4 持久化与可靠性
| ID | 需求 | 优先级 |
|----|------|--------|
| F30 | 消息落盘可恢复；重启后从磁盘完整恢复 | P0 |
| F31 | 崩溃窗口仅丢"未 fsync"的数据；ack=all 时零丢失 | P0 |
| F32 | 段滚动（默认 1GiB）与过期清理（时间/大小） | P1 |
| F33 | 索引损坏可重建，数据损坏可定位（CRC32） | P1 |
| F34 | 消息生命周期（TTL）：Topic 级 `retention.ms` 配置，过期消息不可消费并被后台清理 | P1 |
| F35 | 幂等生产：Broker 维护 `request_id` 滑动窗口幂等表，重复请求返回首次结果（近期去重） | P1 |

### 4.5 高可用（P2 阶段）
| ID | 需求 | 优先级 |
|----|------|--------|
| F40 | 主从复制：Follower 拉取 Leader 段增量 | P2 |
| F41 | 故障切换：Leader 宕机自动选主，消费位点可恢复 | P2 |
| F42 | 生产者/消费者 SDK 支持端点列表与自动重连 | P2 |
| F43 | Broker 心跳与故障检测：节点间周期性心跳，超时触发分区副本重选举 | P2 |

### 4.6 配置化
| ID | 需求 | 优先级 |
|----|------|--------|
| F50 | 配置项：监听地址/端口、数据目录、线程数、fsync 策略、段大小、保留策略、连接上限 | P0 |
| F51 | 配置变更热加载或重启生效（文档说明） | P2 |

## 5. 非功能需求

### 5.1 性能指标
| 指标 | 目标 | 说明 |
|------|------|------|
| 生产吞吐 | ≥ 10w TPS | 单 Broker，batch=1000，3 分区 |
| 消费吞吐 | ≥ 10w TPS | 单 Broker，多分区并行 |
| 生产延迟 | p99 < 5ms / p50 < 1ms | 同机内网，默认 fsync 策略（EVERY_N_MS） |
| 消费延迟 | p99 < 10ms | 长轮询除外 |
| 并发连接 | ≥ 5w | 长连接，空闲连接额外内存 < 1KB |
| 消息大小 | 1B ~ 1MB | 超出拒绝（BAD_REQUEST） |
| 内存上限 | 配置可调（默认写缓冲 512MB） | 超限走背压，不 OOM |
| 单条写入放大 | ≤ 2×（日志 + 索引） | 实测指标，进 bench 基线 |

### 5.2 可用性与可靠性
- 数据可靠性：at-least-once（至少一次投递）；ack=all 语义下**零丢失**。
- 背压策略（明确）：
  - 写侧：写缓冲达阈值（默认 512MB）→ 拒绝新写返回 `RETRY`，同时触发紧急 fsync；
  - 读侧：连接读缓冲达上限（默认 8MB）→ 暂停 `EPOLLIN` 并通知对端窗口暂停；
  - Worker 任务队列满 → 生产者调用阻塞（可配置超时），禁止无界队列。
- 心跳检测：Broker 侧对消费者会话 30s 无心跳判离线；节点间心跳（P2）超时触发副本选举。
- 优雅停机：SIGTERM 时 flush 全部缓冲、关闭监听、安全退出，重放校验不丢已确认数据。
- 故障隔离：单分区存储错误不影响其他分区；单连接异常不影响其他连接。
- 灾难恢复：数据目录完整性由 CRC32 校验 + 索引可重建保证；段文件损坏定位到具体 offset。

### 5.3 安全与运维
- 明文 TCP + 可选简单鉴权（预留 token 校验位），不支持加密（不引入 TLS）。
- 运维工具：`tools/` 提供 topic 管理、数据目录检查、段文件统计。
- 监控指标（内置暴露，`/metrics` 或日志周期输出）：
  - 吞吐：生产/消费 TPS（按 topic 聚合）；
  - 延迟：生产 p50/p99/p999；
  - 队列深度：各分区未消费条数与积压字节；
  - 连接/会话数、Worker 队列水位、写缓冲水位、内存池占用；
  - 存储：段数、清理进度、fsync 次数与耗时、恢复耗时。
- 可观测：结构化日志（spdlog，级别见 AGENTS.md §3.5）+ 上述基础指标。

### 5.4 兼容性与平台
- 平台：Linux 优先（epoll/io_uring）；Windows 可编译（select 兜底），不做性能承诺。
- 编译器：GCC ≥ 9 / Clang ≥ 12；构建：CMake ≥ 3.16。
- 第三方依赖：仅 spdlog + gtest，其余全部自研。

### 5.5 可维护性
- 分层单向依赖（core ← network ← server；client 独立），模块可替换。
- 协议冻结，字段布局向后兼容（版本号预留）。
- 文档与代码同步（见 AGENTS.md 第 4 节）。

## 6. 里程碑与范围

| 阶段 | 范围 | 交付 |
|------|------|------|
| P0 骨架 | 目录、构建、文档 | 本文档 + docs/* + CMake |
| P1 MVP | 单机持久化 Broker + SDK | 生产/消费/管理全链路，压测达标 |
| P2 高可用 | 复制、ack 语义、故障切换 | 多节点部署与切换 |
| P3 生产化 | 监控、限流、配额、管理工具 | 运维完善，生产可用 |

## 7. 明确不做的（Out of Scope）

- 跨数据中心复制、事务消息（两阶段提交）。
- 消息回溯/时间旅行消费（仅支持 offset 定位）。
- TLS 加密、鉴权体系（仅预留 token 位）。
- 死信队列与延迟消息（P1 起按需评估）。

## 8. 成功度量

- 性能指标（§5.1）在 `bench/` 可复现。
- 可靠性：kill -9 场景下数据校验通过（P1 集成测覆盖）。
- 代码质量：`ctest` 全绿 + clang-format 干净 + 无 sanitizer 告警。